# Database - Vercel Postgres (https://vercel.com/docs/storage/vercel-postgres)
# En Vercel, esto se autocompletará desde el Storage tab
DATABASE_URL="postgresql://user:password@localhost:5432/ecomama?schema=public"

# NextAuth.js
# En producción (Vercel), NEXTAUTH_URL se detecta automáticamente
NEXTAUTH_URL="http://localhost:3000"

# Genera un secreto con: openssl rand -base64 32
# IMPORTANTE: Configura esto en Vercel Dashboard > Settings > Environment Variables
AUTH_SECRET=""
